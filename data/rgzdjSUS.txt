<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<?php //sleep(2);
ob_start();
session_start();
require('../funcoes/classes/conexao.php');
require('../funcoes/login/login.php');
//require('../config.php');




$acao = filter_input(INPUT_POST, 'acao', FILTER_SANITIZE_STRING);

switch ($acao) : 
   case 'login' :
     //faz a intera&ccedil;&atilde;o
     $login = filter_input(INPUT_POST, 'login', FILTER_SANITIZE_STRING);
     $senha = filter_input(INPUT_POST, 'senha', FILTER_SANITIZE_STRING);
     if(login($login, $senha)){
     //CRIA A SE&Ccedil;&Atilde;O
     $_SESSION['administrador'] = pegaLogin($login);

     }else{
       $dados = pegaLogin($login);
       if (!$dados) {
       	  echo 'noif';
       }elseif($dados-><html><body><p>administrador_senha != md5(strrev($senha))) {
       	  echo 'diffpass';
       }elseif($dados-&gt;administrador_nivel &gt; 2) {
       	  echo 'non';
       }
     }
  break;

   default:
     echo 'erro';
   break;
endswitch;
ob_end_flush();</p></body></html>
