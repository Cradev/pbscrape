<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>import java.util.Scanner;

public class Test {
    public static void main(String[] args) {

        System.out.println("This program generates a number and if you check it, you win: ");
        System.out.println("If you check the exact number, you win 10,000 &euro;");
        System.out.println("If you check three of the digits, you win 5,000 &euro;");
        System.out.println("If you check only one number, you win 1,000 &euro;");

        Scanner input = new Scanner(System.in);

        String lotteryNumber = "" + (int)(Math.random() * 10) + (int)(Math.random() * 10);

        System.out.println("Please enter your number (two digits) ");

        String numberEntered = input.nextLine();

        if (numberEntered.length() &lt; 0) {
            System.out.println("This is a negative number. Try again!");
            numberEntered = input.nextLine();

        }

        else if (numberEntered.length() &gt; 99) {
            System.out.println("This is not a two digit number. Try again!");
            numberEntered = input.nextLine();

        }

        else if (numberEntered.length() &gt; 0 &amp;&amp; numberEntered.length() &lt; 99) {
            char lotteryDigit1 = lotteryNumber.charAt(0);
            char lotteryDigit2 = lotteryNumber.charAt(1);

            char guessDigit1 = numberEntered.charAt(0);
            char guessDigit2 = numberEntered.charAt(1);

            System.out.println("The lottery number is: " + lotteryNumber);
            System.out.println("Your number is: " + numberEntered);

            if (numberEntered.equals(lotteryNumber)) {
                System.out.println("Exact match! ^o^ You won 10,000 &euro;");

            } else if (guessDigit1 == lotteryDigit1 &amp;&amp; guessDigit2 == lotteryDigit2) {
                System.out.println("Match all digits! You won 3,000 &euro; :)");

            } else if (guessDigit1 == lotteryDigit1 ||
                    guessDigit2 == lotteryDigit1 ||
                    guessDigit1 == lotteryDigit2 ||
                    guessDigit2 == lotteryDigit2) {
                System.out.println("Match one digit: you won 1,000 &euro;.");
            }
            else {
                System.out.println("Sorry, no match. :(");
            }
        }
    }
}</p></body></html>
