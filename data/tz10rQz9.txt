<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>// sortowanie 3 liczb.cpp: Okre&#347;la punkt wej&#347;cia dla aplikacji konsoli.
//

#include "stdafx.h"
#include <conio.h>

int _tmain(int argc, _TCHAR* argv[])
{
	int a,b,c;
	printf("Podaj pierwsza liczbe\n");
	scanf("%d",&amp;a);
	printf("Podaj druga liczbe\n");
	scanf("%d",&amp;b);
	printf("Podaj trzecia liczbe\n");
	scanf("%d",&amp;c);
	if(a&gt;=b)
	{
		if(a&gt;=c)
		{
			if(b&gt;=c)
				printf("Posortowane liczby: %d %d %d",c,b,a);
			else
				printf("Posortowane liczby: %d %d %d",b,c,a);
		}
		else
			printf("Posortowane liczby: %d %d %d",b,a,c);
	}
	else
	{
		if(b&gt;=c)
		{
			if(a&gt;=c)
				printf("Posortowane liczby: %d %d %d",c,a,b);
			else
				printf("Posortowane liczby: %d %d %d",a,c,b);
		}
		else
			printf("Posortowane liczby: %d %d %d",a,b,c);
	}
	
	_getch();
	return 0;
}</conio.h></p></body></html>
