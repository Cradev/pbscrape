<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>// updates the data layer with any changes to units(position, health etc.)
    // the last two lines in this function should always change gameController.currentState to gameController.gameState.AnimateActionPhase and decrement gameController.actionsLeft by 
    public abstract void PerformAction(int x, int y);

    // display each possible target for the ability (we might want this to be in the controller
    public abstract void ShowPossibleMoves(List<tile> moveOptions);

    // update the visual representation of the game board based on the action
    // once all the visuals are done animating, this function must change gameController.currentState to gameController.gameState.ActionPhase if gameController.actionsLeft &gt; 0 and
    // gameController.gameState.EndofTurn if actionsleft = 0
    public abstract void PerformVisuals();

    //highlights the the move to display on the map eg. the tile you are moving to if moving, the tile you are attacking if attacking, the area of effect on an aoe ability
    public abstract void HighlightPotentialMove(Tile t);</tile></p></body></html>
