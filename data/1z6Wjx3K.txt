<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>#include <iostream>
#include <ctime>

using namespace std;

void swap(int *x, int *y){
  int tmp=*x;
  *x=*y;
  *y=tmp;
}

int randpart(int *ptr, int l, int r){
  
  int p=rand()%(r-l+1);

  swap(&amp;ptr[l+p],&amp;ptr[r]);
  
  int rank=l;
  for(int i=l; i&lt;=(r-1); i++)
    if(ptr[i]<ptr swap rank return int kthsmallest l r k if ptr>k){
    return kthsmallest(ptr, l, rank-1, k);
  }
  
  if(rank<k return kthsmallest rank r k int main srand arr n="sizeof(arr)/sizeof(arr[0]);" median cout n-1 k-1></k></ptr></ctime></iostream></p></body></html>
