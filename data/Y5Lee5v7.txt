<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>clc
    clear
    a=input('Wybierz program 1-4');
     switch a
         case 1
             disp('Liczenie sumy kolejnych liczb')
          N=input('ile liczb chcesz wprowadzi&#263;');
          suma=0;
          for i=1:N
              suma=suma+N;
          end
          fprintf('Wynik to %d', suma)
         case 2
          disp('Warto&#347;c srednia z wektora')
          x=0;
          n=input('Wprowadz n ');
          w=rand(1,n);
          for i=1:n
                x=x+w(1,i);
          end
          x=x/n;
          fprintf('Wynik to %.4f', x)
         case 3
          N=input('wprowadz liczb&#281; do policzenia silni');
             if N&gt;=0
                 s=1;
                 for i=1:N
                     s=s*i;
                 end
                 fprintf('Wynik to %d',s)
             else
                 disp('B&#322;ad')
             end
         case 4
    disp('Funkcja kwadratowa');
    pyt=input('Czy chcesz wprowadzi&#263; wspo&#322;czyniki(1) czy u&#380;y&#263; zapisanych w pami&#281;ci(2)');
    v=[-5:5];
    if pyt==2
    a=[1,2,2,0,0,0];
    b=[2,4,3,1,0,0];
    c=[-3,2,2,2,3,0];
    else
        a=input('podaj a');
        b=input('podaj b');
        c=input('podaj c');
    end
    l=1;
    for i=1:size(a,2)
        figure(l)
        hold on
    if a(i)~=0
        delta=b(i)^2-4*a(i)*c(i);
        plot(v,(a(i)*v.^2+b(i)*v+c(i)))
            if delta &gt;= 0
                x1=(-b(i)-sqrt(delta))/(2*a(i));
                x2=(-b(i)+sqrt(delta))/(2*a(i));
                plot(x1,0,'*')
                plot(x2,0,'*')
                if x1==x2
                     disp('2 pierwiastki')
                else
                    disp('1 pierwiastek')
                end
            else
                disp('x nie nale&#380;y do R');
            end
            l=l+1;
    else
        if b(i)~=0
           plot(v,b(i)*v+c(i))
            disp('r&oacute;wnianie lniowe')
            x=-c(i)/b(i);
            plot(x,0,'*')
            l=l+1;
        else
            if c(i)~=0
                disp('r&oacute;wnanie sprzeczne')
            else
                disp('r&oacute;wnanie to&#380;samo&#347;ciowe')
            end
        end
    end
    end
    end</p></body></html>
