<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>def fMax(age):
    return 220 - age

def pIntensity(rest_frequence, age, target_frequence):
    return (target_frequence - rest_frequence) / (fMax(age) - rest_frequence) * 100

print pIntensity(60., 34., 130.)

def computeZone(rest_frequence, age, target_frequence):
    outputMessage = ""
    intensity = pIntensity(rest_frequence, age, target_frequence)
    if 50 &lt; intensity and intensity &lt; 60:
        outputMessage = "Activite moderee"
    elif 60 &lt; intensity and intensity &lt; 70:
        outputMessage = "Perte de graisse"
    elif 70 &lt; intensity and intensity &lt; 80:
        outputMessage = "Aerobie"
    elif 80 &lt; intensity and intensity &lt; 90:
        outputMessage = "Seuil d'anaerobie"
    elif 90 &lt; intensity and intensity &lt; 100:
        outputMessage = " Effort maximal"
    else:
        outputMessage = "out of bound"
        print intensity
    return str(intensity) + outputMessage

print computeZone(60., 40., 140)</p></body></html>
