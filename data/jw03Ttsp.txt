<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body>
<p>import React from 'react';

import Device from '../device';

  const sayHi = (cb, value) =&gt; {
	if ( value ==true) {
	  return (
		</p>
<div><h1> Done done done </h1></div>);
	} else {
	  return (
 		<div><h1> Done 2 done 2 done </h1></div>
		);
	}

  }

export default class Sttnode extends React.Component {
  constructor (props) {
    super(props);
  }

  toggle(value)  {
	  this.refs['device'].setProperty2('d4849de0', 'dev', 'on', true);
   }

  toggle2(value)  {
	  console.log("fsddsfDS");
   }
	
	render () {
	  return (
      <device image="{" typeof this.props.state.current.out="==" undefined : color="#ea6153" ref="device">
         <br>
	 	<button class="ui button" onclick="{()"> this.toggle('true')} &gt;Follow</button>
		  { typeof this.props.state.current.out != 'undefined' ? this.props.state.current.out : '?' }
		  { typeof this.props.state.current.out != 'undefined' ? sayHi(this, true) : sayHi(this, false)} //working
		  { typeof this.props.state.current.out != 'undefined' ? this.toogle(true) : '?' } //not working
		</device>
    );
  }
}</body></html>
