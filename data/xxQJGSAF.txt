<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>/*
 * COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT
 * 
 * 											Made By Simon989
 * 
 * COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT - COPYRIGHT
 */

package me.simon989.main;

import org.bukkit.Bukkit;
import org.bukkit.command.Command;
import org.bukkit.command.CommandSender;
import org.bukkit.entity.Player;
import org.bukkit.plugin.java.JavaPlugin;

import net.md_5.bungee.api.ChatColor;

public class Main extends JavaPlugin {

	public static Main instance;

	public void RepeatingTask() {
		Bukkit.getServer().getScheduler().scheduleSyncRepeatingTask(instance, new Runnable() {
			public void run() {
				for (Player player : Bukkit.getServer().getOnlinePlayers()) {
					if (!instance.getConfig().contains(player.getName().toString())) {
						instance.getConfig().addDefault(player.getName().toString(), 1);
					} else {
						int onlineTime = (int) instance.getConfig().get(player.getName().toString());
						instance.getConfig().set(player.getName().toString(), onlineTime + 1);
					}
				}
			}
		}, 20L, 20L);
	}

	public void onEnable() {
		instance = this;
		RepeatingTask();
	}

	public void onDisable() {
		this.saveConfig();
	}

	public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
		Player player = (Player) sender;
		if (cmd.getName().equalsIgnoreCase("onlinetime")) {
			int time = (int) instance.getConfig().get(player.getName().toString());
			int secs = 0;
			int mins = 0;
			int hours = 0;
			int days = 0;

			if (!(time &gt; 60)) {
				secs = time;
			} else {
				for (int i = time; i &gt; 60; i -= 60) {
					mins++;
					secs = i - 60;
					if (!(i &gt; 60)) {
					}
				}
				if (!(mins &gt; 60)) {
				} else {
					for (int i2 = mins; i2 &gt; 60; i2 -= 60) {
						hours++;
						mins = i2 - 60;
						if (!(i2 &gt; 60)) {
						}
						if (!(hours &gt; 24)) {
						} else {
							for (int i3 = hours; i3 &gt; 24; i3 -= 24) {
								days++;
								hours = i3 - 24;
								if (!(i3 &gt; 24)) {
								}
							}
						}
					}
				}
			}
			player.sendMessage(ChatColor.RED + "You have been online for " + days + " Day(s), " + hours + " Hour(s), "
					+ mins + " Minute(s) &amp; " + secs + " Second(s)!");
		}

		return true;
	}

}</p></body></html>
