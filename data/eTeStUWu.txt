<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.0 Transitional//EN" "http://www.w3.org/TR/REC-html40/loose.dtd">
<html><body><p>import time
import winsound
import os

def Menu():
#    global band
    while True:
        print("\tMEN&Uacute; PRINCIPAL\n\t1.Jugar\n\t2.Instrucciones\n\t3.Salir")
        op = str(input("\tEscriba opci&oacute;n: ").lower())
        if op in ['1','2','3','Jugar', 'jugar', 'Instrucciones',
        "instrucciones", "Salir", 'salir','prueba']:
            return op
        else:
            print('OPCI&Oacute;N INV&Aacute;LIDA, ESCRIBA BIEN LA OPCI&Oacute;N ELEGIDA: ')

def Jugar(op):
	print("DESCIFRAR EL SIGUIENTE MENSAJE")
	print("")
	print(Timer())


    
def Instrucciones(op):
    print("Su misi&oacute;n es poder descifrar"
    "el c&oacute;digo encriptado en cifrado C&eacute;sar")
    

def Salir(op):
    print("Hasta Luego\n\tDEVELOPED BY JAILBREAK")

def cls():
	os.system('cls')

def Timer():
    global s
    global bandtimer
    s = 3
    bandtimer = True
    while bandtimer is True:
    	
    	segundosv="segundos"
    	if s ==1:
    		segundosv="segundo"
    	cls()
    	print('Tiempo:',str(s),segundosv)
    	print("JQNC DGKCMC")
    	rta = input("\tJQNC DGKCMC: ")
    	if rta in ['hola beiaka','HOLA BEIAKA']:
    		bandtimer=False
    	else:
    		print('DESCIFRADO INCORRECTO, ESCRIBA BIEN EL MENSAJE: ')

    	s = s - 1
    	if (s == -1):
    		winsound.PlaySound("SystemHand", winsound.SND_ALIAS)
    		print("\tTIME UP\nSe acabo el tiempo")
    		winsound.PlaySound("SystemHand", winsound.SND_ALIAS)
    		bandtimer = False
    	time.sleep(1)


       
x = True
while x:
    band = True
    op = Menu()
    
    while band:
        
        print("Esto es op: ", op)
        print("ahora vamos al if")


        if (op == "Jugar" or op == "jugar" or op == "1"): 
        	print(Jugar(op))
        	jdn = str(input("\tDesea jugar de nuevo? si/no: ").lower())
        	if jdn in ['si','Si']:
        		Jugar(op)
        	else:
        		if jdn in ['no','No']:
        			band = False
        			x=False
        		else:
        			print('OPCI&Oacute;N INV&Aacute;LIDA, ESCRIBA BIEN LA OPCI&Oacute;N ELEGIDA: ')


        if (op == "Instrucciones" or op == "instrucciones" or op == "2"):
            print(Instrucciones(op))
            band = False
            
            
        if (op == "Salir" or op == "salir" or op == "3"):
            print(Salir(op))
            band = False
            x = False

        if (op=="prueba"):
        	print(Timer())
        	band = False
        	x = False</p></body></html>
